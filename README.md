# Chinese Story Agent (Langflow)

An agentic LLM system built with Langflow that generates Chinese stories using a predefined vocabulary list.

## Features
- Tool-calling agent
- Vocabulary-controlled story generation
- Dockerized setup
- OpenAI GPT-4o integration

## Architecture
Word Loader → Prompt Template → Language Agent → Chat Output

## Setup

```bash
docker compose up -d
